//Select a folder with multiple image folders (no files or folders unrelated to the images should be placed in this folder or in the folder of images .njt files need to be removed) and convert to AVI files

showMessage("Select Open Folder of Directories"); 
openDirDir = getDirectory("Choose a Directory"); 
dirlist = getFileList(openDirDir);

print("openDirDir =", openDirDir);

//Glimpse at the contents of the array "list
Array.show(dirlist); 

//Make Movie directory
movieDir = openDirDir + "Movie/";
File.makeDirectory(movieDir);
print("Movie directory =", movieDir);

//Loop around and process all images sequentially
for (i=0; i<dirlist.length; i++){
	//Open a folder in the specified location
	openDir = openDirDir+dirlist[i];
	list = getFileList(openDir);

	//Make movie
	run("Image Sequence...", "dir=&openDir sort");


	//Get file name
	name = getTitle() + ".avi";
	Savename = movieDir + name;


	//Save AVI movie, 5fps
	run("AVI... ", "compression=JPEG frame=5 save=Savename");

	print("name =", name);

	close();
}
